# Stop packages from running code automatically during install
ignore-scripts=true

# Force npm to run a security audit on every installation
audit=true

# Fail installations or alerts if vulnerabilities meet high or critical thresholds
audit-level=high

# Require a strict lockfile match to prevent silent, unrecorded updates
save-exact=false